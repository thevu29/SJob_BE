spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///path/to/local/config-repo
          default-label: main

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/